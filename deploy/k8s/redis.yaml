apiVersion: apps/v1
kind: Deployment
metadata:
  name: aura-redis
  namespace: aura
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aura-redis
  template:
    metadata:
      labels:
        app: aura-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: aura-redis
  namespace: aura
spec:
  selector:
    app: aura-redis
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
